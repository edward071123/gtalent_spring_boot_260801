# 專案部署到 Linux 步驟

日期：2026-09-03

專案：`gtalent_spring_boot_260801`

部署方式：使用 Spring Boot JAR + systemd 部署到 AWS Ubuntu 的 `/home/ubuntu` 底下。這個專案目前是 Maven 專案，Java 版本是 21，資料庫使用 MySQL，啟動時會透過 Flyway 自動建立/更新資料表。

---

## 1. Linux 主機先安裝必要套件

以下以 Ubuntu/Debian 為例。

```bash
sudo apt update
sudo apt install -y openjdk-21-jdk mysql-server nginx git
```

確認 Java 版本：

```bash
java -version
```

需要看到 Java 21。

---

## 2. 建立 MySQL 資料庫與帳號

進入 MySQL：

```bash
sudo mysql
```

建立正式資料庫與使用者：

```sql
CREATE DATABASE project26_v1 CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'project26_v1'@'localhost' IDENTIFIED BY '請換成正式密碼';
GRANT ALL PRIVILEGES ON project26_v1.* TO 'project26_v1'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

注意：正式密碼不要放到 Git。

---

## 3. 把專案放到 Linux

AWS Ubuntu 建議先放在目前使用者的 home 底下，例如：

```text
/home/ubuntu/gtalent_spring_boot_260801
```

```bash
cd /home/ubuntu
git clone <你的_git_repo_url> gtalent_spring_boot_260801
cd /home/ubuntu/gtalent_spring_boot_260801
```

如果不是用 Git，也可以把整個專案資料夾上傳到：

```text
/home/ubuntu/gtalent_spring_boot_260801
```

---

## 4. 建立正式環境 `.env`

專案會讀取根目錄的 `.env`：

```bash
cp .env.example .env
nano .env
```

正式機建議內容如下：

```properties
DB_NAME=project26_v1
DB_PORT=3306
DB_USERNAME=project26_v1
DB_PASSWORD=請換成正式密碼

TEST_DB_NAME=project26_v1_test
TEST_DB_PORT=3306
TEST_DB_USERNAME=project26_v1_test
TEST_DB_PASSWORD=

GMAIL_USERNAME=你的_gmail
GMAIL_PASSWORD=你的_gmail_app_password

JWT_SECRET=請換成至少32字元以上的隨機密鑰
JWT_ACCESS_TOKEN_SECONDS=900
JWT_REFRESH_TOKEN_SECONDS=604800

APP_BASE_URL=https://你的網域
PASSWORD_RESET_TOKEN_MINUTES=30
```

如果暫時沒有網域，可以先用：

```properties
APP_BASE_URL=http://伺服器IP:8080
```

---

## 5. 打包 Spring Boot JAR

在專案根目錄執行：

```bash
./mvnw clean package
```

如果測試資料庫還沒設定好，暫時可以先跳過測試打包：

```bash
./mvnw clean package -DskipTests
```

打包完成後，JAR 會在：

```text
target/gtalent_spring_boot_260801-0.0.1-SNAPSHOT.jar
```

---

## 6. 先手動啟動確認

```bash
java -jar target/gtalent_spring_boot_260801-0.0.1-SNAPSHOT.jar
```

確認沒有錯誤後，用瀏覽器或 curl 測：

```bash
curl http://localhost:8080
```

第一次啟動時 Flyway 會自動執行：

```text
src/main/resources/db/migration
src/main/resources/db/seeder
```

如果資料表欄位不符合 Entity，因為專案設定是：

```properties
spring.jpa.hibernate.ddl-auto=validate
```

Spring Boot 會直接啟動失敗。這時要修 migration，不要手動亂改正式資料庫。

---

## 7. 建立 systemd 服務

建立服務檔：

```bash
sudo nano /etc/systemd/system/gtalent.service
```

內容：

```ini
[Unit]
Description=Gtalent Spring Boot App
After=network.target mysql.service

[Service]
User=ubuntu
Group=ubuntu
WorkingDirectory=/home/ubuntu/gtalent_spring_boot_260801
ExecStart=/usr/bin/java -jar /home/ubuntu/gtalent_spring_boot_260801/target/gtalent_spring_boot_260801-0.0.1-SNAPSHOT.jar
SuccessExitStatus=143
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

啟動服務：

```bash
sudo systemctl daemon-reload
sudo systemctl enable gtalent
sudo systemctl start gtalent
```

查看狀態：

```bash
sudo systemctl status gtalent
```

查看 log：

```bash
sudo journalctl -u gtalent -f
```

---

## 8. Nginx 反向代理

正式對外網址建議走 Nginx：

```text
使用者 -> https://你的網域/page/login -> Nginx 443 -> Spring Boot 127.0.0.1:8080
```

Spring Boot 本身維持跑在 `8080`，不用直接處理 HTTPS。

建立 Nginx 設定：

```bash
sudo nano /etc/nginx/sites-available/gtalent
```

如果你的正式網域是自己的 domain，例如 `example.com`，`server_name` 就填你的正式網域：

```nginx
server {
    listen 80;
    server_name 你的正式網域;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

如果你暫時只有 AWS 公網 DNS，也可以先填：

```nginx
server {
    listen 80;
    server_name ec2-43-213-68-158.ap-east-2.compute.amazonaws.com;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

啟用設定：

```bash
sudo ln -s /etc/nginx/sites-available/gtalent /etc/nginx/sites-enabled/gtalent
sudo nginx -t
sudo systemctl reload nginx
```

測試 Nginx HTTP：

```bash
curl http://你的正式網域/page/login
```

---

## 9. 之後更新部署

進入專案：

```bash
cd /home/ubuntu/gtalent_spring_boot_260801
```

拉最新程式：

```bash
git pull
```

重新打包：

```bash
./mvnw clean package
```

如果正式機沒有測試資料庫，先用：

```bash
./mvnw clean package -DskipTests
```

重啟服務：

```bash
sudo systemctl restart gtalent
sudo systemctl status gtalent
```

看 log：

```bash
sudo journalctl -u gtalent -f
```

---

## 10. 常見問題檢查

### 8080 被佔用

```bash
sudo lsof -i :8080
```

### MySQL 連不上

確認 MySQL 狀態：

```bash
sudo systemctl status mysql
```

確認 `.env`：

```bash
cat .env
```

確認可以登入：

```bash
mysql -u project26_v1 -p project26_v1
```

### Flyway migration 失敗

看服務 log：

```bash
sudo journalctl -u gtalent -n 200
```

重點看：

```text
Flyway
Migration
Hibernate validate
```

### Gmail 寄信失敗

Gmail 密碼通常要用 App Password，不是一般登入密碼。

`.env` 需要設定：

```properties
GMAIL_USERNAME=你的_gmail
GMAIL_PASSWORD=你的_gmail_app_password
```

---

## 11. 建議正式部署檢查清單

- Linux 已安裝 Java 21
- MySQL 已建立 `project26_v1`
- `.env` 已放在專案根目錄
- `.env` 的 `DB_PASSWORD`、`JWT_SECRET`、`GMAIL_PASSWORD` 已填正式值
- `./mvnw clean package` 可成功
- `java -jar target/gtalent_spring_boot_260801-0.0.1-SNAPSHOT.jar` 可啟動
- `systemctl status gtalent` 是 running
- Nginx `nginx -t` 通過
- 網頁可以透過 IP 或網域打開

---

## 12. 最短部署流程摘要

```bash
sudo apt update
sudo apt install -y openjdk-21-jdk mysql-server nginx git

cd /home/ubuntu
git clone <你的_git_repo_url> gtalent_spring_boot_260801
cd /home/ubuntu/gtalent_spring_boot_260801

cp .env.example .env
nano .env

./mvnw clean package -DskipTests
sudo nano /etc/systemd/system/gtalent.service
sudo systemctl daemon-reload
sudo systemctl enable gtalent
sudo systemctl start gtalent
sudo systemctl status gtalent
```

確認 systemd 啟動正常後，再補 Nginx。
